import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    backgroundColor: "#242424",
    borderRadius: 16,
    padding: 16,
    marginBottom: 10,
    gap: 16,
  },

  title: {
    color: "white",
    fontWeight: "bold",
    fontSize: 18,
  },

  description: {
    color: "#7e7e7e",
    lineHeight: 25,
    fontSize: 15,
  },

  createdAt: {
    color: "#7e7e7e",
    fontSize: 13,
  },
});
